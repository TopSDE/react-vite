// ---------------------------------
// **IMP
// HOOKS => TOGGLE
/*

import React, { useState } from 'react'

const ToggleContent = () => {
    let [toggle, setToggle] = useState(false)

    let handleToggle = () => {
        setToggle(!toggle)
    }

    return (
        <>
            <h1>{toggle ? "Following" : "Unfollowing"}</h1>
            <button onClick={handleToggle}>{toggle ? "UNFOLLOW" : "FOLLOW"}</button>
        </>
    )
}
export default ToggleContent

*/

// ------------------------------------------------------------------

// ONE PROGRAM NOT WRITTEN -- PRESENT IN QTALK

// ------------------------------------------------------------------