// **IMP
// CONTEXT API
/*

import React, {createContext} from 'react'
import WhisherList from '../WhisherList'
import Cart from '../Cart'

export const ProductApi = createContext()

const ProductContext = () => {
    return (
        <ProductApi.Provider value="Provider has sent some Message">
            <WhisherList/>
            <Cart/>
        </ProductApi.Provider>
    )
}
export default ProductContext

*/